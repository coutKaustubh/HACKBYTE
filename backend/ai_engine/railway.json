{
  "services": [
    {
      "name": "app",
      "buildCommand": "npm install && npm run build",
      "startCommand": "npm start && npx prisma migrate deploy --preview-feature"
    },
    {
      "name": "new-db",
      "type": "postgresql",
      "version": "14"
    }
  ]
}